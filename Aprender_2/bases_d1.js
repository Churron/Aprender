const proyecto = {
                                /*Nota personal = Ten en cuenta que el : sirve para crear dentro de las llaves, es decir dentro del objeto las variables
                                pero el = sirve solo afuera por las reglas de node.js es para declarar variables, no es lo mismo :) */
    cliente : "Serambiental",   
    presupuesto : 50000,
    activo :  true

};

console.log(proyecto.cliente)